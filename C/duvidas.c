/*Esses são dois cabeçalhos de bibliotecas padrão do C (.h é de cabeçalho)*/

#include<stdio.h> /* standard input-output header - Define Input e output padrão */
#include<stdlib.h> /* Qual o nome dessa biblioteca?? - Define tipos de variáveis */
#include <conio.h> /*Qual o nome dessa biblioteca?? - Usada para manipular caracteres na tela*/

Porque usar & e por que usar %d o que o Scanf faz (Nível baixo)? scanf("%d", &)
Por que usar int antes? int main()
Por que usar porcentagem? printf("X = %d\n", X)
Por que (raio é double)??    scanf("%lf", &raio);
Por que não consigo fazer exponenciação no C? area=pi*raio*raio;
Por que para limitar as casas decimais tenho que fazer isso (Nível baixo)? printf("A=%.4f\n",area);
Como ler duas variáveis no mesmo scanf dando um 'enter' entre elas??